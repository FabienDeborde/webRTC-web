import React, { memo } from 'react'
import {
  Center
} from '@chakra-ui/core'
// import PropTypes from 'prop-types'

const Home = (props) => {
  return (
    <Center>
      home
    </Center>
  )
}

export default memo(Home)

Home.propTypes = {}
