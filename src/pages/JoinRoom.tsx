import React, { memo } from 'react'
import { RouteComponentProps } from '@reach/router'
// import PropTypes from 'prop-types'

const JoinRoom: React.FC<RouteComponentProps> = () => {
  return <div>JoinRoom</div>
}

export default memo(JoinRoom)

JoinRoom.propTypes = {}
