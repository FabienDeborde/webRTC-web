import React, { memo } from 'react'
import {
  Center
} from '@chakra-ui/core'
// import PropTypes from 'prop-types'

const Home = (props) => {
  return (
    <Center>
      test
    </Center>
  )
}

export default memo(Home)

Home.propTypes = {}
